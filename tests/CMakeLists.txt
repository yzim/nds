function(nds_add_unit_test target)
    add_executable(${target} unit/${ARGN})
    target_compile_options(${target} PRIVATE -Wall -Wextra -Wpedantic -Werror)
    add_test(NAME unit.${target} COMMAND ${target})
    set_tests_properties(unit.${target} PROPERTIES LABELS "unit")
endfunction()

nds_add_unit_test(nds_rdma_wire_test rdma_wire_test.cc)
target_link_libraries(nds_rdma_wire_test PRIVATE nds_wire)
nds_add_unit_test(nds_rdma_path_mtu_test rdma_path_mtu_test.cc)
target_link_libraries(nds_rdma_path_mtu_test PRIVATE nds_rdma_path_mtu)
nds_add_unit_test(nds_npu_ra_qp_test npu_ra_qp_test.cc)
target_link_libraries(nds_npu_ra_qp_test PRIVATE nds_npu_ra_qp)
nds_add_unit_test(nds_aicpu_roce_abi_test aicpu_roce_abi_test.cc)
target_link_libraries(nds_aicpu_roce_abi_test PRIVATE nds_aicpu_roce)
nds_add_unit_test(nds_aiv_roce_abi_test aiv_roce_abi_test.cc)
target_link_libraries(nds_aiv_roce_abi_test PRIVATE nds_aiv_roce)
nds_add_unit_test(nds_logging_test logging_test.cc)
target_link_libraries(nds_logging_test PRIVATE nds_logging)

add_library(nds_fake_runtime SHARED integration/fake_runtime.cc)
target_compile_options(nds_fake_runtime PRIVATE -Wall -Wextra -Wpedantic -Werror)
add_executable(nds_runtime_loader_test integration/runtime_loader_test.cc)
target_link_libraries(nds_runtime_loader_test PRIVATE nds_ra_loader)
target_compile_options(nds_runtime_loader_test PRIVATE -Wall -Wextra -Wpedantic -Werror)
add_test(NAME integration.nds_runtime_loader_test COMMAND nds_runtime_loader_test $<TARGET_FILE:nds_fake_runtime>)
set_tests_properties(integration.nds_runtime_loader_test PROPERTIES LABELS "integration")
add_executable(nds_peer_exchange_test integration/peer_exchange_test.cc)
target_link_libraries(nds_peer_exchange_test PRIVATE nds_peer_exchange)
target_compile_options(nds_peer_exchange_test PRIVATE -Wall -Wextra -Wpedantic -Werror)
add_test(NAME integration.nds_peer_exchange_test COMMAND nds_peer_exchange_test)
set_tests_properties(integration.nds_peer_exchange_test PROPERTIES LABELS "integration")

option(NDS_ENABLE_E2E_TESTS "Register hardware end-to-end tests that require an Ascend NPU and CPU RNIC" OFF)
if(NDS_ENABLE_E2E_TESTS)
    if(NOT TARGET nds_npu_qp_client OR NOT TARGET nds_verbs_server)
        message(FATAL_ERROR "NDS E2E tests require both nds_npu_qp_client and nds_verbs_server")
    endif()
    if(NOT DEFINED NDS_E2E_COMMAND OR NDS_E2E_COMMAND STREQUAL "")
        message(FATAL_ERROR "NDS_E2E_COMMAND must run one bounded real-NPU end-to-end case")
    endif()
    add_test(NAME e2e.npu_to_cpu_write COMMAND ${NDS_E2E_COMMAND})
    set_tests_properties(e2e.npu_to_cpu_write PROPERTIES LABELS "e2e;requires-ascend-npu;requires-rdma" TIMEOUT 120)
endif()
